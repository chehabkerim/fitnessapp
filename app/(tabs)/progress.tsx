import { Placeholder } from '@/components';

export default function Progress() {
  return <Placeholder eyebrow="Progress" title="Progress" body="Weekly and monthly charts for training, weight and steps arrive in a later phase. Each exercise already shows its own history." />;
}
